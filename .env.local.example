# Grand Central Terminus - Environment Variables
# Copy this file to .env.local and fill in your values

# Supabase Configuration (Server-side only)
# No NEXT_PUBLIC_ prefix - used only in API routes and scripts
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (Admin Access - Keep Secret!)
# Get from: Supabase Dashboard → Settings → API → service_role
# WARNING: This key bypasses Row Level Security. Never expose to client.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Admin API Authentication
# Simple bearer token for admin endpoints
# Generate: openssl rand -hex 32
# Usage: Authorization: Bearer your-token-here
ADMIN_API_TOKEN=your-secure-admin-token-here

# Gemini API (for live choice generation)
GEMINI_API_KEY=your-gemini-api-key-here
