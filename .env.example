# Grand Central Terminus - Environment Variables

# Next.js Development Configuration
# Prevents telemetry collection and chunk load errors
NEXT_TELEMETRY_DISABLED=1
WATCHPACK_POLLING=true

# Anthropic Claude API (Server-only for live augmentation)
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Gemini API Key for AI dialogue generation
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Choice Redundancy Filter Configuration
# Threshold for semantic similarity (0-1, default: 0.85)
CHOICE_SIMILARITY_THRESHOLD=0.85

# Semantic Similarity - Set to false to disable embeddings model (faster development)
ENABLE_SEMANTIC_SIMILARITY=false

# Supabase Database Configuration (Server-side only)
# Get from: Supabase Dashboard → Settings → API
# Note: We use server-side only access (no NEXT_PUBLIC_ prefix)
# All database operations happen in API routes, not client-side
SUPABASE_URL=your-supabase-project-url
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_DB_PASSWORD=your-database-password

# Supabase Service Role Key (Admin Database Access - Keep Secret!)
# WARNING: This key bypasses Row Level Security. Never expose to client.
# Get from: Supabase Dashboard → Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin API Token (server-side only, NOT exposed to client)
# Generate a secure random token: openssl rand -base64 32
ADMIN_API_TOKEN=your-secure-admin-token-here

# Optional: API base URL (defaults to current domain)
# NEXT_PUBLIC_API_URL=http://localhost:3003
