# Triage — 30_Lux_Story

If entry flow fails, verify routing and initial data load.
If choices do not advance, check story engine and state transitions.
If ranking dashboard fails, check API response and auth.
If save/resume fails, check storage layer and session keys.
If re-entry loses progress, verify persistence and lookup logic.
